import { test } from './fixtures/isolated-env';

test.describe('FleetGraph assistant drawer', () => {
  test.fixme('opens FleetGraph from the assistant drawer, reviews a delivered finding, and records an action decision', async () => {
    // TODO: seed a delivered FleetGraph finding once the E2E fixture owns FleetGraph setup data.
  });
});
